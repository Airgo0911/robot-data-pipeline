# This copy is kept beside the runnable example for quick inspection.
# Keep it synchronized with ../configs/example.yaml when adapting the schema.
schema_version: "0.1"
action_semantics: "next_state_proxy"
camera_order:
  - wrist_left
  - wrist_right
  - overhead
state_dim: 14
action_dim: 14
quarantine_dir: "outputs/quarantine"
stages:
  - raw
  - lerobot_v2_1
  - dexdata
  - training_view

